{
  "name": "project-cli",
  "version": "1.0.0",
  "description": "Project CLI — Manage dev projects with a secure Web Dashboard",
  "bin": {
    "project-cli": "./server.js",
    "project-terminal": "./cli"
  },
  "scripts": {
    "setup": "node setup-name.js",
    "dashboard": "node server.js",
    "cli": "./cli",
    "start": "node server.js",
    "stop": "pkill -f server.js || true",
    "end": "npm stop"
  },
  "dependencies": {
    "ejs": "^5.0.1",
    "express": "^5.2.1",
    "express-ws": "^5.0.2",
    "multer": "^2.1.1"
  }
}
